# Dependencies (will be installed in container)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (use container env vars instead)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Runtime data (not needed in container)
pids
*.pid
*.seed
*.pid.lock

# Coverage (not needed in container)
coverage/

# Logs (we want to control logging via our logger)
*.log

# Build outputs
dist/
build/
crm-platform/.next/

# Temporary files
*.tmp
*.temp

# Secrets and credentials
api-secrets/
private.key
*.key
*.pem
client_secret_*.json
# INI files (often contain keys)
*.ini

# Git
.git/
.gitignore

# Large/unused dirs (not needed for Docker build; reduces upload size and Windows temp-file issues)
backups/
Apollo/
SUPABASE DOCS/
.trae/
.kiro/
.cursor/
.gemini/
docs/
Twilio Voice Insights/
# Vendor / toolchain (Git Bash etc. – keep repo smaller)
usr/
mingw64/
# Local-only agent/personal files
builder-agent-instructions.md

# Documentation (optional - remove if you want docs in container)
*.md
README.md

# Test files (optional - remove if you want tests in container)
test/
tests/
__tests__/
*.test.js
*.spec.js

# Explicitly include critical files
!server.js
!api/
!package.json
