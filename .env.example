# Copy to .env and set your values
VONAGE_APPLICATION_ID=
VONAGE_FROM_NUMBER=
PORT=5550
# Place your private key as 'private.key' in project root (do not commit)
 
# Alternatively, you can provide the private key contents directly (PEM) in the env var below.
# If set, the backend will use this instead of reading the local private.key file.
VONAGE_PRIVATE_KEY=

# Optional: Additional comma-separated origins to allow in CORS for /api/* endpoints
# Example: https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=
